# Secrets for minecraft-server NixOS host.
#
# Encrypt with sops after setting up age keys:
#   sops hosts/secrets.yaml
#
# Example entries (uncomment and encrypt):
#
# comin/deploy-key: |
#   <ssh-private-key-for-git-pull>
#
# minecraft/rcon-password: |
#   <rcon-password>
